#!/usr/bin/env bash

set -euo pipefail
SRC="$HOME/src/dwm"

[ -d "$SRC" ] || git clone git@github.com:toesocksguy/dwm.git "$SRC"

toolbox run --container dev sh -c "cd '$SRC' && make clean && make"
sudo install "$SRC/dwm" /usr/local/bin/dwm
echo "dwm installed: $(ls -l /usr/local/bin/dwm)"
